# go_cicd_demo

A small Go project to practice:
- Go tooling (fmt, vet, test)
- GitHub Actions CI (no Docker)
- Multi-developer workflows (branches + PRs + reviews)

## Requirements
- Go 1.24+ (`go version`)

## Run
```bash
go run ./cmd/go_cicd_demo 2 3
